// placeholder
import { assertEquals } from "std/assert/mod.ts";
import { placeholder } from "../src/index.ts";

Deno.test("placeholder should return true", () => {
  assertEquals(true, placeholder);
});
